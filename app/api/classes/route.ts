import { NextResponse } from "next/server";

// Placeholder API route. This app uses client-side Supabase only (no backend yet).
export async function GET() {
  return NextResponse.json({ error: "Not implemented" }, { status: 501 });
}
