import { NextResponse } from "next/server";

// Placeholder API route. Assignments are handled client-side for now.
export async function GET() {
  return NextResponse.json({ error: "Not implemented" }, { status: 501 });
}
